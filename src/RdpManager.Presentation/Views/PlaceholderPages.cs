// GroupsPage, ActivityPage and SettingsPage are now real XAML pages (see the .xaml files in this
// folder) so the WinUI XAML type provider registers them for Frame navigation. This file is
// intentionally left without those code-only definitions to avoid duplicate types.
